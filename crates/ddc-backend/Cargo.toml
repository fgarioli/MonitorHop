[package]
name = "ddc-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
log = "0.4"

[target.'cfg(any(windows, target_os = "macos"))'.dependencies]
ddc-hi = "0.4"

[target.'cfg(target_os = "macos")'.dependencies]
ddc-macos = "0.2.2"
